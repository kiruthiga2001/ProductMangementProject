@model IEnumerable<ProductManagement.Models.AttributeValue>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attribute.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Attribute.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AttributeValueId }) |
                @Html.ActionLink("Details", "Details", new { id=item.AttributeValueId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AttributeValueId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
